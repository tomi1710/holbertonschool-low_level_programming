#include "test.h"
/**
 * add - adds two intigers
 * @a: first integer
 * @b: second integer
 * Return: return
 */
int add(int a, int b)
{
	a = a + b;
	return (a);
}

/**
 * sub - substracts b from a
 * @a: first integer
 * @b: second integer
 * Return: return
 */
int sub(int a, int b)
{
	a = a - b;
	return (a);
}

/**
 * mul - multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: return
 */
int mul(int a, int b)
{
	a = a * b;
	return (a);
}

/**
 * div - divides two integers
 * @a: first integer
 * @b: second integer
 * Return: return
 */
int div(int a, int b)
{
	a = a / b;
	return (a);
}

/**
 * div - divides two integers
 * @a: first integer
 * @b: second integer
 * Return: return
 */
int mod(int a, int b)
{
	a = a % b;
	return (a);
}
