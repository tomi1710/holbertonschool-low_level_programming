#include "holberton.h"
/**
 *_isupper - prints 1 if c is upper case
 *@c: lalaslda
 *Return: returns 1 if c is upper case, else returns 0
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
