#include "holberton.h"
/**
 *_strcat - adawdad
 *@dest: adsdawd
 *@src: adawad
 *Return: adawda
 */
char *_strcat(char *dest, char *src)
{
	int i, i2, a;
	for (i = 0; dest[i] != '\0'; i++)
	{
	}
	for (i2 = 0; src[i2] != '\0'; i2++)
	{
	}
	for (a = 0; a != i2; a++)
	{
		dest[i] = src[a];
		i++;
	}
	dest[i + 1] = '\0';
	return (dest);
}
