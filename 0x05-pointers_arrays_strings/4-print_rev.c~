#include "holberton.h"
/**
 *print_rev - sassasas
 *@s: dsjdjsjdd
 */
void print_rev(char *s)
{
  int i;
  for (i = 0; s[i] != '\0'; i++)
    {
    }
  i = i - 1;
  for (i = i; i >= 0; i--)
    {
      _putchar(s[i]);
    }
  _putchar('\n');
}
