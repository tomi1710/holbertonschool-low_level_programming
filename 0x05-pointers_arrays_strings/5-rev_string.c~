#include "holberton.h"
/**
 * rev_string - sasasasass
 * @s: asassasssas
 * Return: sssdsddd
 */
void rev_string(char *s)
{
int i = 0, a = 0, b;
/**while (s[i] != '\0')
{
i++;
}*/
  for (i = 0; s[i] != '\0'; i++)
   {
   }
i--;
while (i > a)
{
b = s[i];
s[i--] = s[a];
s[a++] = b;
}
}
