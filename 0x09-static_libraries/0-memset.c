#include "holberton.h"
/**
 * _memset - sdfg
 * @s: asfd
 * @b: asdfs
 * @n: asdf
 * Return: asfd
 */
char *_memset(char *s, char b, unsigned int n)
{
int i, a;
a = n;
for (i = 0; i < a; i++)
{
s[i] = b;
}
return (s);
}
